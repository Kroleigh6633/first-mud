{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ProgressionCurves",
  "description": "Player + companion progression scaling coefficients. Loaded by ContentProvider into IContentProvider.ProgressionCurves; consumed by Workmanship/CraftingService (skillDivisor) and Companion (layer thresholds). File is OPTIONAL — if absent, ContentProvider falls back to the historical hardcoded constants.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "_note": { "type": "string" },
    "workmanship": {
      "type": "object",
      "additionalProperties": false,
      "required": ["skillDivisor"],
      "properties": {
        "skillDivisor": {
          "type": "integer",
          "minimum": 1,
          "maximum": 100,
          "description": "Divisor applied to craftingSkill when computing the workmanship skill bonus. Lower = more skill contribution. Historical default: 20."
        }
      }
    },
    "companionLayerThresholds": {
      "type": "object",
      "description": "Map of CompanionType name → 6-element threshold array. Index i = UsageCounter required to unlock layer i+1. Index 0 is conventionally 0 (already at layer 1). All 5 CompanionType enum values must be present.",
      "additionalProperties": {
        "type": "array",
        "minItems": 6,
        "maxItems": 6,
        "items": { "type": "integer", "minimum": 0 }
      }
    },
    "craftingSkillScaling": {
      "type": "object",
      "additionalProperties": false,
      "description": "Skill-scaled tolerance on CraftingService quantity check (task #133). tolerance = clamp(baseTolerance + max(0, skillRatio - 1) * tolerancePerSkillTier, baseTolerance, maxTolerance). Block is OPTIONAL — absent means defaults (0.05, 0.02, 0.20).",
      "properties": {
        "_note": { "type": "string" },
        "baseTolerance": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Baseline tolerance (fraction of seeded qty) at skill == requiredSkill. Historical default: 0.05."
        },
        "tolerancePerSkillTier": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Additional tolerance per skill-tier above requirement (one 'tier' = 1.0 of skillRatio above 1.0). Historical default: 0.02."
        },
        "maxTolerance": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Upper clamp on the tolerance. Historical default: 0.20."
        }
      }
    },
    "companionRubberBand": {
      "type": "object",
      "additionalProperties": false,
      "description": "Companion XP catch-up scaling. mult = clamp(1 + max(0, playerLevel - layer*playerLevelWeight) * perGapBonus, 1.0, maxMultiplier). Block is OPTIONAL — absent means defaults (3, 0.15, 3.0) are used.",
      "properties": {
        "_note": { "type": "string" },
        "playerLevelWeight": {
          "type": "integer",
          "minimum": 1,
          "maximum": 20,
          "description": "How many player levels 'equal' one companion layer for gap-measurement purposes. Historical default: 3."
        },
        "perGapBonus": {
          "type": "number",
          "minimum": 0,
          "maximum": 2,
          "description": "XP-multiplier increment per unit of player-level gap. Historical default: 0.15."
        },
        "maxMultiplier": {
          "type": "number",
          "minimum": 1,
          "maximum": 10,
          "description": "Upper clamp on the rubber-band multiplier. Historical default: 3.0."
        }
      }
    }
  }
}
