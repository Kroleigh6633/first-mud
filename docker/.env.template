# Copy to docker/.env and fill in values.
# These are local-dev-only credentials — any values work, but they must match
# the passwords in src/FirstMud.GameServer/appsettings.Development.json.
MSSQL_SA_PASSWORD=FirstMud_Dev#2024
NEO4J_PASSWORD=FirstMud_Neo4j#2024
